#PREFIXDIR  = $(prefix)

INCLUDES = $(GTK2_CFLAGS)

bin_PROGRAMS = keyboard-tray

keyboard_tray_LDADD = $(GTK2_LIBS)

keyboard_tray_SOURCES =                                          \
	keyboard-tray.c
