--- linphone-1.3.5/console/Makefile.am~	2006-07-12 13:12:16.000000000 +0000
+++ linphone-1.3.5/console/Makefile.am	2006-07-12 13:12:29.000000000 +0000
@@ -31,8 +31,8 @@
 
 #in order to workaround a arm-linux cross-compilation libtool bug
 #we need to explicit again the dependencies of the executables:
-if ARMBUILD
+#if ARMBUILD
 AM_LDFLAGS= 	$(top_builddir)/oRTP/src/libortp.la \
 				$(SPEEX_LIBS) \
 				$(OSIP_LIBS)
-endif
+#endif
