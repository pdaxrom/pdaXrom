To list of available configurations use

./build.sh

without arguments.

To clone working directory (for build multiple configurations without copying the same pdaXrom-ng sources) use

./clone-working-dir.sh <new build directory>

