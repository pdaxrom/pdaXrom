#!/bin/sh

PATH=/usr/sbin:/usr/bin:/sbin:/bin

while /bin/true;
do
    killall ps3boot-udev

    /usr/sbin/ps3boot

    mount | grep '/media/dev' | awk '{ print $3 }' | while read f; do
	umount $f && rm -rf $f
    done
done &
