# sudoers file.
#
# This file MUST be edited with the 'visudo' command as root.
#
# See the sudoers man page for the details on how to write a sudoers file.
#

# Host alias specification

# User alias specification

# Cmnd alias specification

# Defaults specification

# Runas alias specification

# User privilege specification
root	ALL=(ALL) ALL

# Uncomment to allow people in group wheel to run all commands
# %wheel	ALL=(ALL)	ALL

# Same thing without a password
# %wheel	ALL=(ALL)	NOPASSWD: ALL

# Samples
# %users  ALL=/sbin/mount /cdrom,/sbin/umount /cdrom
# %users  localhost=/sbin/shutdown -h now
%users ALL=(ALL)	NOPASSWD:/sbin/cardctl,/usr/bin/traceroute,/bin/ping,/sbin/hwclock,/usr/bin/cpuoverclock.py,/etc/sdcontrol,/usr/bin/rotate.sh,/usr/bin/datentime.py,/usr/lib/qt/bin/pppdialer,/usr/bin/apm,/usr/bin/loadkeys,/sbin/modprobe,/usr/lib/qt/bin/qclockchange,/usr/bin/ts_calibrate,/usr/bin/lightnpower.py,/bin/mount,/bin/umount,/sbin/pccardctl,/sbin/hwclock,/sbin/ifconfig,/sbin/insmod,/sbin/iwconfig,/sbin/mkswap,/sbin/poweroff,/sbin/reboot,/sbin/swapon,/sbin/swapoff,/sbin/shutdown,/usr/sbin/alsaconf,/usr/sbin/alsactl,/sbin/setfl,/usr/X11R6/bin/xrandr,/usr/X11R6/bin/xset
zaurus ALL=(ALL)	NOPASSWD: ALL
