config ICEWM
	bool
	prompt "IceWM"

config ICEWM_IMLIB
	bool
	prompt "Use imlib for images"
	depends on ICEWM

config ICEWM_INSTALL
	bool
	prompt "Install in ROM"
	depends on ICEWM

config ICEWM-PDAXROM
	bool
	prompt "IceWM pdaXrom files"
	depends on ICEWM

config ICEWM-PDAXROM_INSTALL
	bool
	prompt "Install in ROM"
	depends on ICEWM-PDAXROM && ICEWM_INSTALL
