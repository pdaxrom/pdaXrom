config SLANG
	bool
	prompt "S-Lang"
	select LIBPNG
	select LIBICONV if SLANG_LIBICONV
	help
	  S-Lang is a C programmer's library that includes routines
	  for the rapid development of sophisticated, user friendly,
	  multi-platform applications.

    config SLANG_LIBICONV
	bool
	prompt "use libiconv"
	depends on SLANG
	help
	  Use libiconv
