config QUAKE1
	bool
	prompt "Quake 1"

config QUAKE1_INSTALL
	bool
	prompt "install in ROM"
	depends on QUAKE1

config QUAKE2
	bool
	prompt "Quake 2"

config QUAKE2_IWMMXT
	bool
	prompt "IWMMXT Optimization"
	depends on QUAKE2 && ARM_ARCH_PXA

config QUAKE2_INSTALL
	bool
	prompt "install in ROM"
	depends on QUAKE2
