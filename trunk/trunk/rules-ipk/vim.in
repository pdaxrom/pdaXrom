config VIM
	bool
	prompt "vim"

config VIM_INSTALL
	bool
	prompt "Install in ROM"
	depends on VIM

config VIM_INSTALL_HELP
	bool
	prompt "Install help files in ROM"
	depends on VIM && VIM_INSTALL

config VIM_INSTALL_SYNTAX
	bool
	prompt "Install syntax files in ROM"
	depends on VIM && VIM_INSTALL
