config FBPANEL-PDAXROM
	bool
	prompt "pdaXrom fbpanel config files"
	depends on FBPANEL

config FBPANEL-PDAXROM_INSTALL
	bool
	prompt "Install in ROM"
	depends on FBPANEL-PDAXROM

config FBPANEL-PDAXROM_VENDOR-INSTALL
	bool
	prompt "Vendor installation"
	depends on FBPANEL-PDAXROM
