config LINKS
	bool
	prompt "links"

config LINKS_GUI
	bool
	prompt "GUI version"
	depends on LINKS && XFREE430

config LINKS_GUI-LIBTIFF
	bool
	prompt "Enable TIFF"
	depends on LINKS_GUI

config LINKS_INSTALL
	bool
	prompt "Install in ROM"
	depends on LINKS
