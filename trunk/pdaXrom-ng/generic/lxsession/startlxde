#!/bin/sh

test -e /etc/profile && . /etc/profile

test -e $HOME/Desktop || mkdir $HOME/Desktop

test -e /usr/bin/dancesplash && dancesplash -b

test -e /usr/bin/fnkey && fnkey

exec /usr/bin/lxsession -s LXDE
